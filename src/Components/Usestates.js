/*import React,{useState} from 'react'

function Usestates() {
  const[count,setCount]=useState(0);

  function toggleClick(){
    setCount(Math.floor(Math.random()*6));
  }

  function togglesClick(){
    setCount(Math.floor(Math.random()*6));
  }
  return (
    <div>
      <h1>{count}</h1>
      <button onClick={toggleClick}>Randoms</button>
      <button onClick={togglesClick}>Randoms</button>

    </div>
  )
}

export default Usestates*/
